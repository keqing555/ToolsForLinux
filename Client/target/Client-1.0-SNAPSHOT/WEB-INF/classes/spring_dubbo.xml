<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans 
https://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/context 
https://www.springframework.org/schema/context/spring-context.xsd
http://dubbo.apache.org/schema/dubbo
http://dubbo.apache.org/schema/dubbo/dubbo.xsd">
    <!--扫描服务层注解-->
    <dubbo:annotation package="com.dubbo.service"></dubbo:annotation>

    <context:component-scan base-package="com.dubbo.service"></context:component-scan>

    <!--定义dubbo应用名称-->
    <dubbo:application name="role_consumer"></dubbo:application>

    <!--设置失败重试次数和全局超时时间-->
    <dubbo:consumer retries="2" timeout="20000"></dubbo:consumer>

    <!--指定接口及方法超时时间-->
    <dubbo:reference timeout="15000" interface="com.dubbo.service.WeaponService">
        <dubbo:method name="getWeaponByRid" timeout="10000"></dubbo:method>
    </dubbo:reference>

    <!--指定注册中心-->
    <dubbo:registry address="zookeeper://192.168.128.128:2181"></dubbo:registry>

    <!--指定要获取的服务-->
    <!--负载均衡策略：轮循（默认随机）-->
    <dubbo:reference id="weaponService" loadbalance="roundrobin"
                     interface="com.dubbo.service.WeaponService"></dubbo:reference>


</beans>